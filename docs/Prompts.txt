====================================================================================================================
git remote -v
git remote add origin https://github.com/kumarlalitss166/Expense-Tracker-Claude-Code.git
git push -u origin HEAD

- Username: your GitHub username
- Password: your access token (not your GitHub password)

====================================================================================================================
Create a "Terms and Conditions" page for Spendly.
1. Add a new route in (app.py] (http://app.py/):
GET /terms - renders templates/terms.html
2. Create templates/terms. html with generic terms and conditions
content appropriate for a personal expense tracking app.
Include sections like: Acceptance of Terms, Use of Service,
User Data, Limitations of Liability, and
Extend base.html if it exists, otherwise landing.html.
3. In templates/landing.html, update the footer link href from "#" to "/terms".


=================================================================================================================
Add a modal to @templates/landing.html that opens when the user
clicks
"See how it works".

Requirements:

- Clicking "See how it works" opens a modal overlay
- Modal contains an embedded YouTube video (use any placeholder
YouTube URL
for now, I will replace it later)
- Video should be playable inside the modal
- Clicking the close button OR clicking outside the modal closes
it
- When the modal closes, the video must stop playing (not
continue in background)
- No page libraries or dependencies - vanilla JS only, since we
are not
using any JS framework in this project

Do not modify any other part of the page.